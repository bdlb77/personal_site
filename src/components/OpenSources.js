import React from "react";
import ArticlesOS from "./styles/ArticlesOS";

const OpenSources = props => (
  <ArticlesOS>
    <OpenSource />
    <OpenSource />
  </ArticlesOS>
);

const OpenSource = props => <div>Open Source</div>;
export default OpenSources;
